AWSTemplateFormatVersion: 2010-09-09
Description: An example CloudFormation template for Fargate.
Parameters:
  AutoScalingTargetValue:
    Type: Number
    Default: 50
  CertificateStackName:
    Type: String
    Default: ""
  ContainersInTaskDefinition:
    Description: How many Container(x) are defined in parameters
    Type: Number
  Container1ALBListenerRuleType:
    Description: https://docs.aws.amazon.com/elasticloadbalancing/latest/application/load-balancer-listeners.html#rule-condition-types
    Type: String
    Default: ""
    AllowedValues:
      - "host-header"
      - ""
  Container1ALBListenerRuleValue:
    Description: https://docs.aws.amazon.com/elasticloadbalancing/latest/application/load-balancer-listeners.html#rule-condition-types
    Type: String
    Default: ""
  Container1HealthCheckPath:
    Description: URI to test for container healthiness
    Type: String
    Default: ""
  Container1ContainerName:
    Description: Name of container to run in Task Instance
    Type: String
    Default: ""
  Container1ImageArn:
    Description: ARN of image to use for container in Task Defintion
    Type: String
    Default: ""
  Container1ImageStackName:
    Description: Name of stack which created the ECR repository holding the container image
    Type: String
    Default: ""
  Container1ImageTag:
    Description: Image tag to be used in for this container
    Type: String
    Default: "latest"
  Container1Port:
    Description: Port to expose from the Task Instance
    Type: Number
    Default: 0
  Container1SecretsStackName:
    Description: Stack used to store secret environment variables for the container
    Type: String
    Default: ""
  Container2ALBListenerRuleType:
    Description: https://docs.aws.amazon.com/elasticloadbalancing/latest/application/load-balancer-listeners.html#rule-condition-types
    Type: String
    Default: ""
    AllowedValues:
      - "host-header"
      - ""
  Container2ALBListenerRuleValue:
    Description: https://docs.aws.amazon.com/elasticloadbalancing/latest/application/load-balancer-listeners.html#rule-condition-types
    Type: String
    Default: ""
  Container2HealthCheckPath:
    Description: URI to test for container healthiness
    Type: String
    Default: ""
  Container2ContainerName:
    Description: Name of container to run in Task Instance
    Type: String
    Default: ""
  Container2ImageArn:
    Description: ARN of image to use for container in Task Defintion
    Type: String
    Default: ""
  Container2ImageStackName:
    Description: Name of stack which created the ECR repository holding the container image
    Type: String
    Default: ""
  Container2ImageTag:
    Description: Image tag to be used in for this container
    Type: String
    Default: "latest"
  Container2Port:
    Description: Port to expose from the Task Instance
    Type: Number
    Default: 0
  Container2SecretsStackName:
    Description: Stack used to store secret environment variables for the container
    Type: String
    Default: ""
  Container3ALBListenerRuleType:
    Description: https://docs.aws.amazon.com/elasticloadbalancing/latest/application/load-balancer-listeners.html#rule-condition-types
    Type: String
    Default: ""
    AllowedValues:
      - "host-header"
      - ""
  Container3ALBListenerRuleValue:
    Description: https://docs.aws.amazon.com/elasticloadbalancing/latest/application/load-balancer-listeners.html#rule-condition-types
    Type: String
    Default: ""
  Container3HealthCheckPath:
    Description: URI to test for container healthiness
    Type: String
    Default: ""
  Container3ContainerName:
    Description: Name of container to run in Task Instance
    Type: String
    Default: ""
  Container3ImageArn:
    Description: ARN of image to use for container in Task Defintion
    Type: String
    Default: ""
  Container3ImageStackName:
    Description: Name of stack which created the ECR repository holding the container image
    Type: String
    Default: ""
  Container3ImageTag:
    Description: Image tag to be used in for this container
    Type: String
    Default: "latest"
  Container3Port:
    Description: Port to expose from the Task Instance
    Type: Number
    Default: 0
  Container3SecretsStackName:
    Description: Stack used to store secret environment variables for the container
    Type: String
    Default: ""
  Container4ALBListenerRuleType:
    Description: https://docs.aws.amazon.com/elasticloadbalancing/latest/application/load-balancer-listeners.html#rule-condition-types
    Type: String
    Default: ""
    AllowedValues:
      - "host-header"
      - ""
  Container4ALBListenerRuleValue:
    Description: https://docs.aws.amazon.com/elasticloadbalancing/latest/application/load-balancer-listeners.html#rule-condition-types
    Type: String
    Default: ""
  Container4HealthCheckPath:
    Description: URI to test for container healthiness
    Type: String
    Default: ""
  Container4ContainerName:
    Description: Name of container to run in Task Instance
    Type: String
    Default: ""
  Container4ImageArn:
    Description: ARN of image to use for container in Task Defintion
    Type: String
    Default: ""
  Container4ImageStackName:
    Description: Name of stack which created the ECR repository holding the container image
    Type: String
    Default: ""
  Container4ImageTag:
    Description: Image tag to be used in for this container
    Type: String
    Default: "latest"
  Container4Port:
    Description: Port to expose from the Task Instance
    Type: Number
    Default: 0
  Container4SecretsStackName:
    Description: Stack used to store secret environment variables for the container
    Type: String
    Default: ""
  Container5ALBListenerRuleType:
    Description: https://docs.aws.amazon.com/elasticloadbalancing/latest/application/load-balancer-listeners.html#rule-condition-types
    Type: String
    Default: ""
    AllowedValues:
      - "host-header"
      - ""
  Container5ALBListenerRuleValue:
    Description: https://docs.aws.amazon.com/elasticloadbalancing/latest/application/load-balancer-listeners.html#rule-condition-types
    Type: String
    Default: ""
  Container5HealthCheckPath:
    Description: URI to test for container healthiness
    Type: String
    Default: ""
  Container5ContainerName:
    Description: Name of container to run in Task Instance
    Type: String
    Default: ""
  Container5ImageArn:
    Description: ARN of image to use for container in Task Defintion
    Type: String
    Default: ""
  Container5ImageStackName:
    Description: Name of stack which created the ECR repository holding the container image
    Type: String
    Default: ""
  Container5ImageTag:
    Description: Image tag to be used in for this container
    Type: String
    Default: "latest"
  Container5Port:
    Description: Port to expose from the Task Instance
    Type: Number
    Default: 0
  Container5SecretsStackName:
    Description: Stack used to store secret environment variables for the container
    Type: String
    Default: ""
  Container6ALBListenerRuleType:
    Description: https://docs.aws.amazon.com/elasticloadbalancing/latest/application/load-balancer-listeners.html#rule-condition-types
    Type: String
    Default: ""
    AllowedValues:
      - "host-header"
      - ""
  Container6ALBListenerRuleValue:
    Description: https://docs.aws.amazon.com/elasticloadbalancing/latest/application/load-balancer-listeners.html#rule-condition-types
    Type: String
    Default: ""
  Container6HealthCheckPath:
    Description: URI to test for container healthiness
    Type: String
    Default: ""
  Container6ContainerName:
    Description: Name of container to run in Task Instance
    Type: String
    Default: ""
  Container6ImageArn:
    Description: ARN of image to use for container in Task Defintion
    Type: String
    Default: ""
  Container6ImageStackName:
    Description: Name of stack which created the ECR repository holding the container image
    Type: String
    Default: ""
  Container6ImageTag:
    Description: Image tag to be used in for this container
    Type: String
    Default: "latest"
  Container6Port:
    Description: Port to expose from the Task Instance
    Type: Number
    Default: 0
  Container6SecretsStackName:
    Description: Stack used to store secret environment variables for the container
    Type: String
    Default: ""
  Container7ALBListenerRuleType:
    Description: https://docs.aws.amazon.com/elasticloadbalancing/latest/application/load-balancer-listeners.html#rule-condition-types
    Type: String
    Default: ""
    AllowedValues:
      - "host-header"
      - ""
  Container7ALBListenerRuleValue:
    Description: https://docs.aws.amazon.com/elasticloadbalancing/latest/application/load-balancer-listeners.html#rule-condition-types
    Type: String
    Default: ""
  Container7HealthCheckPath:
    Description: URI to test for container healthiness
    Type: String
    Default: ""
  Container7ContainerName:
    Description: Name of container to run in Task Instance
    Type: String
    Default: ""
  Container7ImageArn:
    Description: ARN of image to use for container in Task Defintion
    Type: String
    Default: ""
  Container7ImageStackName:
    Description: Name of stack which created the ECR repository holding the container image
    Type: String
    Default: ""
  Container7ImageTag:
    Description: Image tag to be used in for this container
    Type: String
    Default: "latest"
  Container7Port:
    Description: Port to expose from the Task Instance
    Type: Number
    Default: 0
  Container7SecretsStackName:
    Description: Stack used to store secret environment variables for the container
    Type: String
    Default: ""
  Container8ALBListenerRuleType:
    Description: https://docs.aws.amazon.com/elasticloadbalancing/latest/application/load-balancer-listeners.html#rule-condition-types
    Type: String
    Default: ""
    AllowedValues:
      - "host-header"
      - ""
  Container8ALBListenerRuleValue:
    Description: https://docs.aws.amazon.com/elasticloadbalancing/latest/application/load-balancer-listeners.html#rule-condition-types
    Type: String
    Default: ""
  Container8HealthCheckPath:
    Description: URI to test for container healthiness
    Type: String
    Default: ""
  Container8ContainerName:
    Description: Name of container to run in Task Instance
    Type: String
    Default: ""
  Container8ImageArn:
    Description: ARN of image to use for container in Task Defintion
    Type: String
    Default: ""
  Container8ImageStackName:
    Description: Name of stack which created the ECR repository holding the container image
    Type: String
    Default: ""
  Container8ImageTag:
    Description: Image tag to be used in for this container
    Type: String
    Default: "latest"
  Container8Port:
    Description: Port to expose from the Task Instance
    Type: Number
    Default: 0
  Container8SecretsStackName:
    Description: Stack used to store secret environment variables for the container
    Type: String
    Default: ""
  Container9ALBListenerRuleType:
    Description: https://docs.aws.amazon.com/elasticloadbalancing/latest/application/load-balancer-listeners.html#rule-condition-types
    Type: String
    Default: ""
    AllowedValues:
      - "host-header"
      - ""
  Container9ALBListenerRuleValue:
    Description: https://docs.aws.amazon.com/elasticloadbalancing/latest/application/load-balancer-listeners.html#rule-condition-types
    Type: String
    Default: ""
  Container9HealthCheckPath:
    Description: URI to test for container healthiness
    Type: String
    Default: ""
  Container9ContainerName:
    Description: Name of container to run in Task Instance
    Type: String
    Default: ""
  Container9ImageArn:
    Description: ARN of image to use for container in Task Defintion
    Type: String
    Default: ""
  Container9ImageStackName:
    Description: Name of stack which created the ECR repository holding the container image
    Type: String
    Default: ""
  Container9ImageTag:
    Description: Image tag to be used in for this container
    Type: String
    Default: "latest"
  Container9Port:
    Description: Port to expose from the Task Instance
    Type: Number
    Default: 0
  Container9SecretsStackName:
    Description: Stack used to store secret environment variables for the container
    Type: String
    Default: ""
  Container10ALBListenerRuleType:
    Description: https://docs.aws.amazon.com/elasticloadbalancing/latest/application/load-balancer-listeners.html#rule-condition-types
    Type: String
    Default: ""
    AllowedValues:
      - "host-header"
      - ""
  Container10ALBListenerRuleValue:
    Description: https://docs.aws.amazon.com/elasticloadbalancing/latest/application/load-balancer-listeners.html#rule-condition-types
    Type: String
    Default: ""
  Container10HealthCheckPath:
    Description: URI to test for container healthiness
    Type: String
    Default: ""
  Container10ContainerName:
    Description: Name of container to run in Task Instance
    Type: String
    Default: ""
  Container10ImageArn:
    Description: ARN of image to use for container in Task Defintion
    Type: String
    Default: ""
  Container10ImageStackName:
    Description: Name of stack which created the ECR repository holding the container image
    Type: String
    Default: ""
  Container10ImageTag:
    Description: Image tag to be used in for this container
    Type: String
    Default: "latest"
  Container10Port:
    Description: Port to expose from the Task Instance
    Type: Number
    Default: 0
  Container10SecretsStackName:
    Description: Stack used to store secret environment variables for the container
    Type: String
    Default: ""
  ContainersHaveMemoryReservations:
    Type: String
    Default: "false"
    AllowedValues:
      - "true"
      - "false"
  DesiredTaskInstancesInService:
    Type: Number
    Default: 1
  ECSTaskDefCPU:
    Description: The number of cpu units used by the task.
    Type: String
    Default: ""
  ECSTaskDefMemory:
    Description: The amount (in MiB) of memory used by the task.
    Type: String
    Default: ""
  ECSClusterStackName:
    Description: Name of the stack which created the ECS Cluster
    Type: String
  ECSServiceDeploymentController:
    Description: The deployment controller type to use. There are three deployment controller types available
    Type: String
    Default: ECS
    AllowedValues:
      - ECS
      - CODE_DEPLOY
      - EXTERNAL
  EnableFirelens:
    Description: Enable log streaming through Kinesis Firehose
    Type: String
    Default: "false"
    AllowedValues:
      - "true"
      - "false"
  EnableFluentd:
    Description: Enable log streaming through Fluentd
    Type: String
    Default: "false"
    AllowedValues:
      - "true"
      - "false"
  LoadBalancerEnableHttp2Routing:
    Description:  Indicates whether HTTP/2 is enabled. The possible values are true and false.
    Type: String
    Default: "true"
    AllowedValues:
      - "true"
      - "false"
  LoadBalancerEnableLogging:
    Description: Enable logging of ALB access logs to S3
    Type: String
    Default: "false"
    AllowedValues:
      - "true"
      - "false"
  ECSTaskDefEphemeralStorageSizeInGB:
    Description: Size of the ephemeral stroage to be associated with the Task Definition
    Type: Number
    Default: 20
    MinValue: 20
    MaxValue: 200
  HostedZoneId:
    Type: String
    Default: ""
  HostedZoneName:
    Type: String
    Default: ""
  LaunchType:
    Type: String
    Default: FARGATE
    AllowedValues:
      - FARGATE
      - EC2
  LoadBalancerStickinessType:
    Type: String
    Default: ''
    AllowedValues:
      - lb_cookie
      - app_cookie
      - source_ip
      - source_ip_dest_ip
      - source_ip_dest_ip_proto
      - ''
  LoadBalancerType:
    Type: String
    Default: ""
    AllowedValues:
      - ""
      - alb-internal
  MatcherHttpCode:
    Description: Specifies the HTTP codes that healthy targets must use when responding to an HTTP health check.
    Type: String
    Default: 200-299,301,302
  MaxTaskInstancesInService:
    Type: Number
    Default: 10
  MinTaskInstancesInService:
    Type: Number
    Default: 2
  NetworkMode:
    Type: String
    Description: The Docker networking mode to use for the containers in the task.
    Default: awsvpc
    AllowedValues:
      - awsvpc
      - bridge
      - host
      - none
  LoadBalancerHttpPort:
    Type: Number
    Default: 0
  LoadBalancerHttpsPort:
    Type: Number
    Default: 0
  ECSTaskDefCpuArchitecture:
    Description: X86_64 or ARM64 for the task defintion
    Type: String
    Default: X86_64
    AllowedValues:
      - X86_64
      - ARM64
  SecurityGroupStackName:
    Type: String
    Default: ""
  SecurityGroupStackExportVariableName:
    Type: String
    Default: SecurityGroupId
  ServiceName:
    Description: Name given to the ECS Service
    Type: String
  Subdomain:
    Type: String
    Default: ""
  VPCStackName:
    Description: Name of the stack used to create the VPC
    Type: String
Conditions:
  CreateEphemeralStorageCondition: !Not 
    - !Equals 
      - !Ref ECSTaskDefEphemeralStorageSizeInGB
      - 20
  CreateLoadBalancerCondition: !Not
    - !Equals 
      - !Ref LoadBalancerType
      - ""
  CreatePublicHTTPListener: !And 
    - !Condition CreateLoadBalancerCondition
    - !Not 
      - !Equals 
        - !Ref LoadBalancerHttpPort
        - 0
  CreatePublicHTTPSListener: !And 
    - !Condition CreateLoadBalancerCondition
    - !Not 
      - !Equals 
        - !Ref LoadBalancerHttpsPort
        - 0
  CreateRoute53RecordSet: !And 
    - !Condition CreateLoadBalancerCondition
    - !Not  
      - !Equals 
        - !Ref Subdomain
        - ""
    - !Not  
      - !Equals 
        - !Ref HostedZoneName
        - ""
    - !Not  
      - !Equals 
        - !Ref HostedZoneId
        - ""
  EnableFirelensCondition: !Equals 
    - !Ref EnableFirelens
    - "true"
  EnableFluentdCondition: !Equals 
    - !Ref EnableFluentd
    - "true"
  IsFargateLaunchType: !Equals 
    - !Ref LaunchType
    - "FARGATE"
  IsECSServiceDeploymentControllerUsingECS: !Equals 
    - !Ref ECSServiceDeploymentController
    - "ECS"
  LoadBalancerEnableLoggingCondition: !Equals 
    - !Ref LoadBalancerEnableLogging
    - true
  TaskDefitionContainer1HasExposedPort: !Not 
    - !Equals 
      - !Ref Container1Port
      - 0
  TaskDefitionContainer1HasHealthCheck: !Not 
    - !Equals 
      - !Ref Container1HealthCheckPath
      - ""
  TaskDefitionContainer1HasSecretStack: !Not 
    - !Equals 
      - !Ref Container1SecretsStackName
      - ""
  TaskDefitionContainer1UsesImageArn: !Not 
    - !Equals 
      - !Ref Container1ImageArn
      - ""
  TaskDefitionContainer2HasExposedPort: !Not 
    - !Equals 
      - !Ref Container2Port
      - 0
  TaskDefitionContainer2HasHealthCheck: !Not 
    - !Equals 
      - !Ref Container2HealthCheckPath
      - ""
  TaskDefitionContainer2HasSecretStack: !Not 
    - !Equals 
      - !Ref Container2SecretsStackName
      - ""
  TaskDefitionContainer2UsesImageArn: !Not 
    - !Equals 
      - !Ref Container2ImageArn
      - ""
  TaskDefitionContainer3HasExposedPort: !Not 
    - !Equals 
      - !Ref Container3Port
      - 0
  TaskDefitionContainer3HasHealthCheck: !Not 
    - !Equals 
      - !Ref Container3HealthCheckPath
      - ""
  TaskDefitionContainer3HasSecretStack: !Not 
    - !Equals 
      - !Ref Container3SecretsStackName
      - ""
  TaskDefitionContainer3UsesImageArn: !Not 
    - !Equals 
      - !Ref Container3ImageArn
      - ""
  TaskDefitionContainer4HasExposedPort: !Not 
    - !Equals 
      - !Ref Container4Port
      - 0
  TaskDefitionContainer4HasHealthCheck: !Not 
    - !Equals 
      - !Ref Container4HealthCheckPath
      - ""
  TaskDefitionContainer4HasSecretStack: !Not 
    - !Equals 
      - !Ref Container4SecretsStackName
      - ""
  TaskDefitionContainer4UsesImageArn: !Not 
    - !Equals 
      - !Ref Container4ImageArn
      - ""
  TaskDefitionContainer5HasExposedPort: !Not 
    - !Equals 
      - !Ref Container5Port
      - 0
  TaskDefitionContainer5HasHealthCheck: !Not 
    - !Equals 
      - !Ref Container5HealthCheckPath
      - ""
  TaskDefitionContainer5HasSecretStack: !Not 
    - !Equals 
      - !Ref Container5SecretsStackName
      - ""
  TaskDefitionContainer5UsesImageArn: !Not 
    - !Equals 
      - !Ref Container5ImageArn
      - ""
  TaskDefitionContainer6HasExposedPort: !Not 
    - !Equals 
      - !Ref Container6Port
      - 0
  TaskDefitionContainer6HasHealthCheck: !Not 
    - !Equals 
      - !Ref Container6HealthCheckPath
      - ""
  TaskDefitionContainer6HasSecretStack: !Not 
    - !Equals 
      - !Ref Container6SecretsStackName
      - ""
  TaskDefitionContainer6UsesImageArn: !Not 
    - !Equals 
      - !Ref Container6ImageArn
      - ""
  TaskDefitionContainer7HasExposedPort: !Not 
    - !Equals 
      - !Ref Container7Port
      - 0
  TaskDefitionContainer7HasHealthCheck: !Not 
    - !Equals 
      - !Ref Container7HealthCheckPath
      - ""
  TaskDefitionContainer7HasSecretStack: !Not 
    - !Equals 
      - !Ref Container7SecretsStackName
      - ""
  TaskDefitionContainer7UsesImageArn: !Not 
    - !Equals 
      - !Ref Container7ImageArn
      - ""
  TaskDefitionContainer8HasExposedPort: !Not 
    - !Equals 
      - !Ref Container8Port
      - 0
  TaskDefitionContainer8HasHealthCheck: !Not 
    - !Equals 
      - !Ref Container8HealthCheckPath
      - ""
  TaskDefitionContainer8HasSecretStack: !Not 
    - !Equals 
      - !Ref Container8SecretsStackName
      - ""
  TaskDefitionContainer8UsesImageArn: !Not 
    - !Equals 
      - !Ref Container8ImageArn
      - ""
  TaskDefitionContainer9HasExposedPort: !Not 
    - !Equals 
      - !Ref Container9Port
      - 0
  TaskDefitionContainer9HasHealthCheck: !Not 
    - !Equals 
      - !Ref Container9HealthCheckPath
      - ""
  TaskDefitionContainer9HasSecretStack: !Not 
    - !Equals 
      - !Ref Container9SecretsStackName
      - ""
  TaskDefitionContainer9UsesImageArn: !Not 
    - !Equals 
      - !Ref Container9ImageArn
      - ""
  TaskDefitionContainer10HasExposedPort: !Not 
    - !Equals 
      - !Ref Container10Port
      - 0
  TaskDefitionContainer10HasHealthCheck: !Not 
    - !Equals 
      - !Ref Container10HealthCheckPath
      - ""
  TaskDefitionContainer10HasSecretStack: !Not 
    - !Equals 
      - !Ref Container10SecretsStackName
      - ""
  TaskDefitionContainer10UsesImageArn: !Not 
    - !Equals 
      - !Ref Container10ImageArn
      - ""
  TaskDefinitionHas10TargetContainers: !Equals 
    - !Ref ContainersInTaskDefinition
    - 10
  TaskDefinitionHasAtLeast9TargetContainers: !Or 
    - !Condition TaskDefinitionHas10TargetContainers
    - !Equals 
      - !Ref ContainersInTaskDefinition
      - 9
  TaskDefinitionHasAtLeast8TargetContainers: !Or 
    - !Condition TaskDefinitionHasAtLeast9TargetContainers
    - !Equals 
      - !Ref ContainersInTaskDefinition
      - 8
  TaskDefinitionHasAtLeast7TargetContainers: !Or 
    - !Condition TaskDefinitionHasAtLeast8TargetContainers
    - !Equals 
      - !Ref ContainersInTaskDefinition
      - 7
  TaskDefinitionHasAtLeast6TargetContainers: !Or 
    - !Condition TaskDefinitionHasAtLeast7TargetContainers
    - !Equals 
      - !Ref ContainersInTaskDefinition
      - 6
  TaskDefinitionHasAtLeast5TargetContainers: !Or 
    - !Condition TaskDefinitionHasAtLeast6TargetContainers
    - !Equals 
      - !Ref ContainersInTaskDefinition
      - 5
  TaskDefinitionHasAtLeast4TargetContainers: !Or 
    - !Condition TaskDefinitionHasAtLeast5TargetContainers
    - !Equals 
      - !Ref ContainersInTaskDefinition
      - 4
  TaskDefinitionHasAtLeast3TargetContainers: !Or 
    - !Condition TaskDefinitionHasAtLeast4TargetContainers
    - !Equals 
      - !Ref ContainersInTaskDefinition
      - 3
  TaskDefinitionHasAtLeast2TargetContainers: !Or 
    - !Condition TaskDefinitionHasAtLeast3TargetContainers
    - !Equals 
      - !Ref ContainersInTaskDefinition
      - 2
  TaskDefinitionHasAtLeast1TargetContainers: !Or 
    - !Condition TaskDefinitionHasAtLeast2TargetContainers
    - !Equals 
      - !Ref ContainersInTaskDefinition
      - 1
  UseIPTargetType: !Equals
    - !Ref NetworkMode
    - "awsvpc"
Mappings:
  RegionARNPrefix:
    us-east-1:
      ARNPrefix: "arn:aws:"
    us-west-1:
      ARNPrefix: "arn:aws:"
    us-west-2:
      ARNPrefix: "arn:aws:"
    eu-west-1:
      ARNPrefix: "arn:aws:"
    ap-northeast-1:
      ARNPrefix: "arn:aws:"
    ap-northeast-2:
      ARNPrefix: "arn:aws:"
    ap-southeast-1:
      ARNPrefix: "arn:aws:"
    ap-southeast-2:
      ARNPrefix: "arn:aws:"
    ap-south-1:
      ARNPrefix: "arn:aws:"
    us-east-2:
      ARNPrefix: "arn:aws:"
    sa-east-1:
      ARNPrefix: "arn:aws:"
    cn-north-1:
      ARNPrefix: "arn:aws-cn:"
    eu-central-1:
      ARNPrefix: "arn:aws:"
  RegionELBAccountId:
    us-east-1:
      AccountId: "127311923021"
    us-west-1:
      AccountId: "027434742980"
    us-west-2:
      AccountId: "797873946194"
    eu-west-1:
      AccountId: "156460612806"
    ap-northeast-1:
      AccountId: "582318560864"
    ap-northeast-2:
      AccountId: "600734575887"
    ap-southeast-1:
      AccountId: "114774131450"
    ap-southeast-2:
      AccountId: "783225319266"
    ap-south-1:
      AccountId: "718504428378"
    us-east-2:
      AccountId: "033677994240"
    sa-east-1:
      AccountId: "507241528517"
    cn-north-1:
      AccountId: "638102146993"
    eu-central-1:
      AccountId: "054676820928"
Resources:
  TaskDefinition:
    Type: AWS::ECS::TaskDefinition
    Properties:
      # Name of the task definition. Subsequent versions of the task definition are grouped together under this name.
      Family: !Join ["-", [!Ref ServiceName, TaskDefinition1]]
      # awsvpc is required for Fargate
      NetworkMode: !Ref NetworkMode
      # 256 (.25 vCPU) - Available memory values: 0.5GB, 1GB, 2GB
      # 512 (.5 vCPU) - Available memory values: 1GB, 2GB, 3GB, 4GB
      # 1024 (1 vCPU) - Available memory values: 2GB, 3GB, 4GB, 5GB, 6GB, 7GB, 8GB
      # 2048 (2 vCPU) - Available memory values: Between 4GB and 16GB in 1GB increments
      # 4096 (4 vCPU) - Available memory values: Between 8GB and 30GB in 1GB increments
      Cpu: !Ref ECSTaskDefCPU
      # 0.5GB, 1GB, 2GB - Available cpu values: 256 (.25 vCPU)
      # 1GB, 2GB, 3GB, 4GB - Available cpu values: 512 (.5 vCPU)
      # 2GB, 3GB, 4GB, 5GB, 6GB, 7GB, 8GB - Available cpu values: 1024 (1 vCPU)
      # Between 4GB and 16GB in 1GB increments - Available cpu values: 2048 (2 vCPU)
      # Between 8GB and 30GB in 1GB increments - Available cpu values: 4096 (4 vCPU)
      Memory: !Ref ECSTaskDefMemory
      EphemeralStorage: !If
        - CreateEphemeralStorageCondition
        - SizeInGiB: !Ref ECSTaskDefEphemeralStorageSizeInGB
        - !Ref AWS::NoValue
      # A role needed by ECS.
      # "The ARN of the task execution role that containers in this task can assume. All containers in this task are granted the permissions that are specified in this role."
      # "There is an optional task execution IAM role that you can specify with Fargate to allow your Fargate tasks to make API calls to Amazon ECR."
      ExecutionRoleArn: !GetAtt TaskExecutionRole.Arn
      # "The Amazon Resource Name (ARN) of an AWS Identity and Access Management (IAM) role that grants containers in the task permission to call AWS APIs on your behalf."
      TaskRoleArn: !Ref TaskRole
      RuntimePlatform: 
        CpuArchitecture: !Ref ECSTaskDefCpuArchitecture
      ContainerDefinitions:
        - !If
          - EnableFirelensCondition
          - Name: log_router
            Image: !Sub "906394416424.dkr.ecr.${AWS::Region}.amazonaws.com/aws-for-fluent-bit:latest"
            Essential: true
            FirelensConfiguration:
              Type: fluentbit
            LogConfiguration:
              LogDriver: awslogs
              Options:
                awslogs-region: !Ref AWS::Region
                awslogs-group: !Ref FirelensLogGroup
                awslogs-stream-prefix: firelens
                mode: non-blocking
            MemoryReservation: 50
          - !Ref AWS::NoValue
        - !If
          - TaskDefinitionHasAtLeast1TargetContainers
          - Name: !Ref Container1ContainerName
            Image: !Join
              - ":"
              - - !Ref Container1ImageArn
                - !Ref Container1ImageTag
            PortMappings: !If
              - TaskDefitionContainer1HasExposedPort
              - - ContainerPort: !Ref Container1Port
                  HostPort: !Ref Container1Port
              - !Ref AWS::NoValue
            Environment:
              - Name: dbserver
                Value: 192.168.1.105
              - Name: PLAY_HTTP_PORT
                Value: 9000
            # Send logs to CloudWatch Logs
            Secrets:
              - !If
                - TaskDefitionContainer1HasSecretStack
                - Name: ENV_FROM_JSON
                  ValueFrom:
                    Fn::ImportValue:
                      !Join ["-", [!Ref Container2SecretsStackName, "SecretArn"]]
                - !Ref AWS::NoValue
            LogConfiguration: 
              LogDriver: awslogs
              Options:
                awslogs-region: !Ref AWS::Region
                awslogs-group: !Ref LogGroup
                awslogs-stream-prefix: ecs
                mode: non-blocking
          - !Ref AWS::NoValue
        - !If
          - TaskDefinitionHasAtLeast2TargetContainers
          - Name: !Ref Container2ContainerName
            Image: !Join
              - ":"
              - - !Ref Container2ImageArn
                - !Ref Container2ImageTag
            PortMappings: !If
              - TaskDefitionContainer2HasExposedPort
              - - ContainerPort: !Ref Container2Port
                  HostPort: !Ref Container2Port
              - !Ref AWS::NoValue
            Environment:
              - Name: dbserver
                Value: 192.168.1.105
              - Name: PLAY_HTTP_PORT
                Value: 9001
            # Send logs to CloudWatch Logs
            Secrets:
              - !If
                - TaskDefitionContainer2HasSecretStack
                - Name: ENV_FROM_JSON
                  ValueFrom:
                    Fn::ImportValue:
                      !Join ["-", [!Ref Container2SecretsStackName, "SecretArn"]]
                - !Ref AWS::NoValue
            LogConfiguration: 
              LogDriver: awslogs
              Options:
                awslogs-region: !Ref AWS::Region
                awslogs-group: !Ref LogGroup
                awslogs-stream-prefix: ecs
                mode: non-blocking
          - !Ref AWS::NoValue
        - !If
          - TaskDefinitionHasAtLeast3TargetContainers
          - Name: !Ref Container3ContainerName
            Image: !Join
              - ":"
              - - !Ref Container3ImageArn
                - !Ref Container3ImageTag
            PortMappings: !If
              - TaskDefitionContainer3HasExposedPort
              - - ContainerPort: !Ref Container3Port
                  HostPort: !Ref Container3Port
              - !Ref AWS::NoValue
            Environment:
              - Name: dbserver
                Value: 192.168.1.105
              - Name: PLAY_HTTP_PORT
                Value: 9002
            # Send logs to CloudWatch Logs
            Secrets:
              - !If
                - TaskDefitionContainer3HasSecretStack
                - Name: ENV_FROM_JSON
                  ValueFrom:
                    Fn::ImportValue:
                      !Join ["-", [!Ref Container3SecretsStackName, "SecretArn"]]
                - !Ref AWS::NoValue
            LogConfiguration: 
              LogDriver: awslogs
              Options:
                awslogs-region: !Ref AWS::Region
                awslogs-group: !Ref LogGroup
                awslogs-stream-prefix: ecs
                mode: non-blocking
          - !Ref AWS::NoValue
        - !If
          - TaskDefinitionHasAtLeast4TargetContainers
          - Name: !Ref Container4ContainerName
            Image: !Join
              - ":"
              - - !Ref Container4ImageArn
                - !Ref Container4ImageTag
            PortMappings: !If
              - TaskDefitionContainer4HasExposedPort
              - - ContainerPort: !Ref Container4Port
                  HostPort: !Ref Container4Port
              - !Ref AWS::NoValue
            Environment:
              - Name: dbserver
                Value: 192.168.1.105
              - Name: PLAY_HTTP_PORT
                Value: 9003
            # Send logs to CloudWatch Logs
            Secrets:
              - !If
                - TaskDefitionContainer4HasSecretStack
                - Name: ENV_FROM_JSON
                  ValueFrom:
                    Fn::ImportValue:
                      !Join ["-", [!Ref Container4SecretsStackName, "SecretArn"]]
                - !Ref AWS::NoValue
            LogConfiguration: 
              LogDriver: awslogs
              Options:
                awslogs-region: !Ref AWS::Region
                awslogs-group: !Ref LogGroup
                awslogs-stream-prefix: ecs
                mode: non-blocking
          - !Ref AWS::NoValue
        - !If
          - TaskDefinitionHasAtLeast5TargetContainers
          - Name: !Ref Container5ContainerName
            Image: !Join
              - ":"
              - - !Ref Container5ImageArn
                - !Ref Container5ImageTag
            PortMappings: !If
              - TaskDefitionContainer5HasExposedPort
              - - ContainerPort: !Ref Container5Port
                  HostPort: !Ref Container5Port
              - !Ref AWS::NoValue
            Environment:
              - Name: dbserver
                Value: 192.168.1.105
              - Name: PLAY_HTTP_PORT
                Value: 9004
            # Send logs to CloudWatch Logs
            Secrets:
              - !If
                - TaskDefitionContainer5HasSecretStack
                - Name: ENV_FROM_JSON
                  ValueFrom:
                    Fn::ImportValue:
                      !Join ["-", [!Ref Container5SecretsStackName, "SecretArn"]]
                - !Ref AWS::NoValue
            LogConfiguration: 
              LogDriver: awslogs
              Options:
                awslogs-region: !Ref AWS::Region
                awslogs-group: !Ref LogGroup
                awslogs-stream-prefix: ecs
                mode: non-blocking
          - !Ref AWS::NoValue
        - !If
          - TaskDefinitionHasAtLeast6TargetContainers
          - Name: !Ref Container6ContainerName
            Image: !Join
              - ":"
              - - !Ref Container6ImageArn
                - !Ref Container6ImageTag
            PortMappings: !If
              - TaskDefitionContainer6HasExposedPort
              - - ContainerPort: !Ref Container6Port
                  HostPort: !Ref Container6Port
              - !Ref AWS::NoValue
            Environment:
              - Name: dbserver
                Value: 192.168.1.105
              - Name: PLAY_HTTP_PORT
                Value: 9005
            # Send logs to CloudWatch Logs
            Secrets:
              - !If
                - TaskDefitionContainer6HasSecretStack
                - Name: ENV_FROM_JSON
                  ValueFrom:
                    Fn::ImportValue:
                      !Join ["-", [!Ref Container6SecretsStackName, "SecretArn"]]
                - !Ref AWS::NoValue
            LogConfiguration: 
              LogDriver: awslogs
              Options:
                awslogs-region: !Ref AWS::Region
                awslogs-group: !Ref LogGroup
                awslogs-stream-prefix: ecs
                mode: non-blocking
          - !Ref AWS::NoValue
        - !If
          - TaskDefinitionHasAtLeast7TargetContainers
          - Name: !Ref Container7ContainerName
            Image: !Join
              - ":"
              - - !Ref Container7ImageArn
                - !Ref Container7ImageTag
            PortMappings: !If
              - TaskDefitionContainer7HasExposedPort
              - - ContainerPort: !Ref Container7Port
                  HostPort: !Ref Container7Port
              - !Ref AWS::NoValue
            Environment:
              - Name: dbserver
                Value: 192.168.1.105
              - Name: PLAY_HTTP_PORT
                Value: 9006
            # Send logs to CloudWatch Logs
            Secrets:
              - !If
                - TaskDefitionContainer7HasSecretStack
                - Name: ENV_FROM_JSON
                  ValueFrom:
                    Fn::ImportValue:
                      !Join ["-", [!Ref Container7SecretsStackName, "SecretArn"]]
                - !Ref AWS::NoValue
            LogConfiguration: 
              LogDriver: awslogs
              Options:
                awslogs-region: !Ref AWS::Region
                awslogs-group: !Ref LogGroup
                awslogs-stream-prefix: ecs
                mode: non-blocking
          - !Ref AWS::NoValue
        - !If
          - TaskDefinitionHasAtLeast8TargetContainers
          - Name: !Ref Container8ContainerName
            Image: !Join
              - ":"
              - - !Ref Container8ImageArn
                - !Ref Container8ImageTag
            PortMappings: !If
              - TaskDefitionContainer8HasExposedPort
              - - ContainerPort: !Ref Container8Port
                  HostPort: !Ref Container8Port
              - !Ref AWS::NoValue
            Environment:
              - Name: dbserver
                Value: 192.168.1.105
              - Name: PLAY_HTTP_PORT
                Value: 9007
            # Send logs to CloudWatch Logs
            Secrets:
              - !If
                - TaskDefitionContainer8HasSecretStack
                - Name: ENV_FROM_JSON
                  ValueFrom:
                    Fn::ImportValue:
                      !Join ["-", [!Ref Container8SecretsStackName, "SecretArn"]]
                - !Ref AWS::NoValue
            LogConfiguration: 
              LogDriver: awslogs
              Options:
                awslogs-region: !Ref AWS::Region
                awslogs-group: !Ref LogGroup
                awslogs-stream-prefix: ecs
                mode: non-blocking
          - !Ref AWS::NoValue
        - !If
          - TaskDefinitionHasAtLeast9TargetContainers
          - Name: !Ref Container9ContainerName
            Image: !Join
              - ":"
              - - !Ref Container9ImageArn
                - !Ref Container9ImageTag
            PortMappings: !If
              - TaskDefitionContainer9HasExposedPort
              - - ContainerPort: !Ref Container9Port
                  HostPort: !Ref Container9Port
              - !Ref AWS::NoValue
            Environment:
              - Name: dbserver
                Value: 192.168.1.105
              - Name: PLAY_HTTP_PORT
                Value: 9008
            # Send logs to CloudWatch Logs
            Secrets:
              - !If
                - TaskDefitionContainer9HasSecretStack
                - Name: ENV_FROM_JSON
                  ValueFrom:
                    Fn::ImportValue:
                      !Join ["-", [!Ref Container9SecretsStackName, "SecretArn"]]
                - !Ref AWS::NoValue
            LogConfiguration: 
              LogDriver: awslogs
              Options:
                awslogs-region: !Ref AWS::Region
                awslogs-group: !Ref LogGroup
                awslogs-stream-prefix: ecs
                mode: non-blocking
          - !Ref AWS::NoValue
        - !If
          - TaskDefinitionHas10TargetContainers
          - Name: !Ref Container10ContainerName
            Image: !Join
              - ":"
              - - !Ref Container10ImageArn
                - !Ref Container10ImageTag
            PortMappings: !If
              - TaskDefitionContainer10HasExposedPort
              - - ContainerPort: !Ref Container10Port
                  HostPort: !Ref Container10Port
              - !Ref AWS::NoValue
            Environment:
              - Name: dbserver
                Value: 192.168.1.105
              - Name: PLAY_HTTP_PORT
                Value: 9009
            # Send logs to CloudWatch Logs
            Secrets:
              - !If
                - TaskDefitionContainer10HasSecretStack
                - Name: ENV_FROM_JSON
                  ValueFrom:
                    Fn::ImportValue:
                      !Join ["-", [!Ref Container10SecretsStackName, "SecretArn"]]
                - !Ref AWS::NoValue
            LogConfiguration: 
              LogDriver: awslogs
              Options:
                awslogs-region: !Ref AWS::Region
                awslogs-group: !Ref LogGroup
                awslogs-stream-prefix: ecs
                mode: non-blocking
          - !Ref AWS::NoValue
  # A role needed by ECS
  TaskExecutionRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: !Join ["", [!Ref ServiceName, ExecutionRole]]
      AssumeRolePolicyDocument:
        Statement:
          - Effect: Allow
            Principal:
              Service: ecs-tasks.amazonaws.com
            Action: "sts:AssumeRole"
      Policies:
        - PolicyName: AllowECRContainerPull
          PolicyDocument:
            Version: 2012-10-17
            Statement:
              - Effect: Allow
                Action: "ecr:*"
                Resource: "*"
        - PolicyName: AllowRetrieveSecrets
          PolicyDocument:
            Version: 2012-10-17
            Statement:
              - Effect: Allow
                Action:
                  - "secretsmanager:GetSecretValue"
                  - "kms:Decrypt"
                Resource:
                  - "*"
      ManagedPolicyArns:
        - "arn:aws:iam::aws:policy/service-role/AmazonECSTaskExecutionRolePolicy"
  # A role for the containers
  TaskRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: !Join ["", [!Ref ServiceName, TaskRole]]
      AssumeRolePolicyDocument:
        Statement:
          - Effect: Allow
            Principal:
              Service: ecs-tasks.amazonaws.com
            Action: "sts:AssumeRole"
      # ManagedPolicyArns:
      #   -
      Policies:
        - PolicyName: AllEcsExecPolicy
          PolicyDocument:
            Version: 2012-10-17
            Statement:
              - Effect: Allow
                Action:
                  - "ecs:ExecuteCommand"
                  - "ssmmessages:CreateControlChannel"
                  - "ssmmessages:CreateDataChannel"
                  - "ssmmessages:OpenControlChannel"
                  - "ssmmessages:OpenDataChannel"
                Resource:
                  - "*"
        - PolicyName: AllowRetrieveSecrets
          PolicyDocument:
            Version: 2012-10-17
            Statement:
              - Effect: Allow
                Action:
                  - "secretsmanager:GetSecretValue"
                  - "kms:Decrypt"
                Resource:
                  - "*"
  # A role needed for auto scaling
  AutoScalingRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: !Join ["", [!Ref ServiceName, AutoScalingRole]]
      AssumeRolePolicyDocument:
        Statement:
          - Effect: Allow
            Principal:
              Service: ecs-tasks.amazonaws.com
            Action: "sts:AssumeRole"
      ManagedPolicyArns:
        - "arn:aws:iam::aws:policy/service-role/AmazonEC2ContainerServiceAutoscaleRole"
  LoadBalancerSecurityGroup:
    Type: AWS::EC2::SecurityGroup
    Condition: CreateLoadBalancerCondition
    Properties:
      GroupDescription:
        !Join ["", [!Ref ServiceName, LoadBalancerSecurityGroup]]
      VpcId:
        Fn::ImportValue: !Join ["-", [!Ref VPCStackName, "VPCID"]]
      SecurityGroupIngress:
        - !If
          - CreatePublicHTTPListener
          - IpProtocol: tcp
            FromPort: !Ref LoadBalancerHttpPort
            ToPort: !Ref LoadBalancerHttpPort
            CidrIp: 0.0.0.0/0
          - !Ref AWS::NoValue
        - !If
          - CreatePublicHTTPSListener
          - IpProtocol: tcp
            FromPort: !Ref LoadBalancerHttpsPort
            ToPort: !Ref LoadBalancerHttpsPort
            CidrIp: 0.0.0.0/0
          - !Ref AWS::NoValue
  SecurityGroupStackNameLBIngress:
    Type: AWS::EC2::SecurityGroupIngress
    Condition: CreateLoadBalancerCondition
    Properties:
      Description: Ingress from load balancer to ECS cluster servers
      GroupId:
        Fn::ImportValue:
          !Join [
            "-",
            [
              !Ref SecurityGroupStackName,
              !Ref SecurityGroupStackExportVariableName,
            ],
          ]
      IpProtocol: "-1"
      SourceSecurityGroupId: !Ref "LoadBalancerSecurityGroup"
  Service:
    Type: AWS::ECS::Service
    # This dependency is needed so that the load balancer is setup correctly in time
    DependsOn: 
      - ListenerRule1
      - ListenerRule2
      - ListenerRule3
      - ListenerRule4
    Properties:
      ServiceName: !Ref ServiceName
      EnableExecuteCommand: true
      Cluster:
        Fn::ImportValue: !Join ["-", [!Ref ECSClusterStackName, "Cluster"]]
      TaskDefinition: !Ref TaskDefinition
      DeploymentConfiguration: !If 
        - IsECSServiceDeploymentControllerUsingECS
        - MinimumHealthyPercent: 100
          MaximumPercent: 200
          DeploymentCircuitBreaker: 
            Enable: true
            Rollback: true
        - !Ref AWS::NoValue
      DeploymentController:
        Type: !Ref ECSServiceDeploymentController
      DesiredCount: !Ref DesiredTaskInstancesInService
      # This may need to be adjusted if the container takes a while to start up
      HealthCheckGracePeriodSeconds: !If
        - CreateLoadBalancerCondition
        - 30
        - !Ref AWS::NoValue
      LaunchType: !Ref LaunchType
      NetworkConfiguration: 
        AwsvpcConfiguration:
          # change to DISABLED if you're using private subnets that have access to a NAT gateway
          AssignPublicIp: DISABLED
          Subnets:
            - Fn::ImportValue:
                !Join ["-", [!Ref VPCStackName, "PrivateSubnet1AID"]]
            - Fn::ImportValue:
                !Join ["-", [!Ref VPCStackName, "PrivateSubnet2AID"]]
          SecurityGroups:
            - Fn::ImportValue:
                !Join [
                  "-",
                  [
                    !Ref SecurityGroupStackName,
                    !Ref SecurityGroupStackExportVariableName,
                  ],
                ]
      LoadBalancers: 
        - !If
          - CreateLoadBalancerCondition
          - !If
            - TaskDefitionContainer1HasExposedPort
            - ContainerName: !Ref Container1ContainerName
              ContainerPort: !Ref Container1Port
              TargetGroupArn: !Ref TargetGroup1
            - !Ref AWS::NoValue
          - !Ref AWS::NoValue
        - !If
          - CreateLoadBalancerCondition
          - !If
            - TaskDefitionContainer2HasExposedPort
            - ContainerName: !Ref Container2ContainerName
              ContainerPort: !Ref Container2Port
              TargetGroupArn: !Ref TargetGroup2
            - !Ref AWS::NoValue
          - !Ref AWS::NoValue
        - !If
          - CreateLoadBalancerCondition
          - !If
            - TaskDefitionContainer3HasExposedPort
            - ContainerName: !Ref Container3ContainerName
              ContainerPort: !Ref Container3Port
              TargetGroupArn: !Ref TargetGroup3
            - !Ref AWS::NoValue
          - !Ref AWS::NoValue
        - !If
          - CreateLoadBalancerCondition
          - !If
            - TaskDefitionContainer4HasExposedPort
            - ContainerName: !Ref Container4ContainerName
              ContainerPort: !Ref Container4Port
              TargetGroupArn: !Ref TargetGroup4
            - !Ref AWS::NoValue
          - !Ref AWS::NoValue
        - !If
          - CreateLoadBalancerCondition
          - !If
            - TaskDefitionContainer5HasExposedPort
            - ContainerName: !Ref Container5ContainerName
              ContainerPort: !Ref Container5Port
              TargetGroupArn: !Ref TargetGroup5
            - !Ref AWS::NoValue
          - !Ref AWS::NoValue
        - !If
          - CreateLoadBalancerCondition
          - !If
            - TaskDefitionContainer6HasExposedPort
            - ContainerName: !Ref Container6ContainerName
              ContainerPort: !Ref Container6Port
              TargetGroupArn: !Ref TargetGroup6
            - !Ref AWS::NoValue
          - !Ref AWS::NoValue
        - !If
          - CreateLoadBalancerCondition
          - !If
            - TaskDefitionContainer7HasExposedPort
            - ContainerName: !Ref Container7ContainerName
              ContainerPort: !Ref Container7Port
              TargetGroupArn: !Ref TargetGroup7
            - !Ref AWS::NoValue
          - !Ref AWS::NoValue
        - !If
          - CreateLoadBalancerCondition
          - !If
            - TaskDefitionContainer8HasExposedPort
            - ContainerName: !Ref Container8ContainerName
              ContainerPort: !Ref Container8Port
              TargetGroupArn: !Ref TargetGroup8
            - !Ref AWS::NoValue
          - !Ref AWS::NoValue
        - !If
          - CreateLoadBalancerCondition
          - !If
            - TaskDefitionContainer9HasExposedPort
            - ContainerName: !Ref Container9ContainerName
              ContainerPort: !Ref Container9Port
              TargetGroupArn: !Ref TargetGroup9
            - !Ref AWS::NoValue
          - !Ref AWS::NoValue
        - !If
          - CreateLoadBalancerCondition
          - !If
            - TaskDefitionContainer10HasExposedPort
            - ContainerName: !Ref Container10ContainerName
              ContainerPort: !Ref Container10Port
              TargetGroupArn: !Ref TargetGroup10
            - !Ref AWS::NoValue
          - !Ref AWS::NoValue
      PlatformVersion: !If
        - IsFargateLaunchType
        - 1.4.0
        - !Ref AWS::NoValue
      
  TargetGroup1:
    Type: AWS::ElasticLoadBalancingV2::TargetGroup
    Condition: TaskDefitionContainer1HasExposedPort
    Properties:
      HealthCheckIntervalSeconds: 60
      # will look for a 200 status code by default unless specified otherwise
      HealthCheckPath: !If
        - TaskDefitionContainer1HasHealthCheck
        - !Ref Container1HealthCheckPath
        - !Ref AWS::NoValue
      HealthCheckTimeoutSeconds: 10
      UnhealthyThresholdCount: 5
      HealthyThresholdCount: 2
      Matcher:
        HttpCode: !Ref MatcherHttpCode
      Name: !Join ["-", [!Ref Container1ContainerName, TG]]
      Port: !Ref Container1Port
      Protocol: HTTP
      TargetGroupAttributes:
        - Key: deregistration_delay.timeout_seconds
          Value: "60" # default is 300
      TargetType: !If
        - UseIPTargetType
        - ip
        - instance
      VpcId:
        Fn::ImportValue: !Join ["-", [!Ref VPCStackName, "VPCID"]]
  TargetGroup2:
    Type: AWS::ElasticLoadBalancingV2::TargetGroup
    Condition: TaskDefitionContainer2HasExposedPort
    Properties:
      HealthCheckIntervalSeconds: 60
      # will look for a 200 status code by default unless specified otherwise
      HealthCheckPath: !If
        - TaskDefitionContainer2HasHealthCheck
        - !Ref Container2HealthCheckPath
        - !Ref AWS::NoValue
      HealthCheckTimeoutSeconds: 10
      UnhealthyThresholdCount: 5
      HealthyThresholdCount: 2
      Matcher:
        HttpCode: !Ref MatcherHttpCode
      Name: !Join ["-", [!Ref Container2ContainerName, TG]]
      Port: !Ref Container2Port
      Protocol: HTTP
      TargetGroupAttributes:
        - Key: deregistration_delay.timeout_seconds
          Value: "60" # default is 300
      TargetType: !If
        - UseIPTargetType
        - ip
        - instance
      VpcId:
        Fn::ImportValue: !Join ["-", [!Ref VPCStackName, "VPCID"]]
  TargetGroup3:
    Type: AWS::ElasticLoadBalancingV2::TargetGroup
    Condition: TaskDefitionContainer3HasExposedPort
    Properties:
      HealthCheckIntervalSeconds: 60
      # will look for a 200 status code by default unless specified otherwise
      HealthCheckPath: !If
        - TaskDefitionContainer3HasHealthCheck
        - !Ref Container3HealthCheckPath
        - !Ref AWS::NoValue
      HealthCheckTimeoutSeconds: 10
      UnhealthyThresholdCount: 5
      HealthyThresholdCount: 2
      Matcher:
        HttpCode: !Ref MatcherHttpCode
      Name: !Join ["-", [!Ref Container3ContainerName, TG]]
      Port: !Ref Container3Port
      Protocol: HTTP
      TargetGroupAttributes:
        - Key: deregistration_delay.timeout_seconds
          Value: "60" # default is 300
      TargetType: !If
        - UseIPTargetType
        - ip
        - instance
      VpcId:
        Fn::ImportValue: !Join ["-", [!Ref VPCStackName, "VPCID"]]
  TargetGroup4:
    Type: AWS::ElasticLoadBalancingV2::TargetGroup
    Condition: TaskDefitionContainer4HasExposedPort
    Properties:
      HealthCheckIntervalSeconds: 60
      # will look for a 200 status code by default unless specified otherwise
      HealthCheckPath: !If
        - TaskDefitionContainer4HasHealthCheck
        - !Ref Container4HealthCheckPath
        - !Ref AWS::NoValue
      HealthCheckTimeoutSeconds: 10
      UnhealthyThresholdCount: 5
      HealthyThresholdCount: 2
      Matcher:
        HttpCode: !Ref MatcherHttpCode
      Name: !Join ["-", [!Ref Container4ContainerName, TG]]
      Port: !Ref Container4Port
      Protocol: HTTP
      TargetGroupAttributes:
        - Key: deregistration_delay.timeout_seconds
          Value: "60" 
      TargetType: !If
        - UseIPTargetType
        - ip
        - instance
      VpcId:
        Fn::ImportValue: !Join ["-", [!Ref VPCStackName, "VPCID"]]
  TargetGroup5:
    Type: AWS::ElasticLoadBalancingV2::TargetGroup
    Condition: TaskDefitionContainer5HasExposedPort
    Properties:
      HealthCheckIntervalSeconds: 60
      # will look for a 200 status code by default unless specified otherwise
      HealthCheckPath: !If
        - TaskDefitionContainer5HasHealthCheck
        - !Ref Container5HealthCheckPath
        - !Ref AWS::NoValue
      HealthCheckTimeoutSeconds: 10
      UnhealthyThresholdCount: 5
      HealthyThresholdCount: 2
      Matcher:
        HttpCode: !Ref MatcherHttpCode
      Name: !Join ["-", [!Ref Container5ContainerName, TG]]
      Port: !Ref Container5Port
      Protocol: HTTP
      TargetGroupAttributes:
        - Key: deregistration_delay.timeout_seconds
          Value: "60" 
      TargetType: !If
        - UseIPTargetType
        - ip
        - instance
      VpcId:
        Fn::ImportValue: !Join ["-", [!Ref VPCStackName, "VPCID"]]
  TargetGroup6:
    Type: AWS::ElasticLoadBalancingV2::TargetGroup
    Condition: TaskDefitionContainer6HasExposedPort
    Properties:
      HealthCheckIntervalSeconds: 60
      # will look for a 200 status code by default unless specified otherwise
      HealthCheckPath: !If
        - TaskDefitionContainer6HasHealthCheck
        - !Ref Container6HealthCheckPath
        - !Ref AWS::NoValue
      HealthCheckTimeoutSeconds: 10
      UnhealthyThresholdCount: 5
      HealthyThresholdCount: 2
      Matcher:
        HttpCode: !Ref MatcherHttpCode
      Name: !Join ["-", [!Ref Container6ContainerName, TG]]
      Port: !Ref Container6Port
      Protocol: HTTP
      TargetGroupAttributes:
        - Key: deregistration_delay.timeout_seconds
          Value: "60" 
      TargetType: !If
        - UseIPTargetType
        - ip
        - instance
      VpcId:
        Fn::ImportValue: !Join ["-", [!Ref VPCStackName, "VPCID"]]
  TargetGroup7:
    Type: AWS::ElasticLoadBalancingV2::TargetGroup
    Condition: TaskDefitionContainer7HasExposedPort
    Properties:
      HealthCheckIntervalSeconds: 60
      # will look for a 200 status code by default unless specified otherwise
      HealthCheckPath: !If
        - TaskDefitionContainer7HasHealthCheck
        - !Ref Container7HealthCheckPath
        - !Ref AWS::NoValue
      HealthCheckTimeoutSeconds: 10
      UnhealthyThresholdCount: 5
      HealthyThresholdCount: 2
      Matcher:
        HttpCode: !Ref MatcherHttpCode
      Name: !Join ["-", [!Ref Container7ContainerName, TG]]
      Port: !Ref Container7Port
      Protocol: HTTP
      TargetGroupAttributes:
        - Key: deregistration_delay.timeout_seconds
          Value: "60" 
      TargetType: !If
        - UseIPTargetType
        - ip
        - instance
      VpcId:
        Fn::ImportValue: !Join ["-", [!Ref VPCStackName, "VPCID"]]
  TargetGroup8:
    Type: AWS::ElasticLoadBalancingV2::TargetGroup
    Condition: TaskDefitionContainer8HasExposedPort
    Properties:
      HealthCheckIntervalSeconds: 60
      # will look for a 200 status code by default unless specified otherwise
      HealthCheckPath: !If
        - TaskDefitionContainer8HasHealthCheck
        - !Ref Container8HealthCheckPath
        - !Ref AWS::NoValue
      HealthCheckTimeoutSeconds: 10
      UnhealthyThresholdCount: 5
      HealthyThresholdCount: 2
      Matcher:
        HttpCode: !Ref MatcherHttpCode
      Name: !Join ["-", [!Ref Container8ContainerName, TG]]
      Port: !Ref Container8Port
      Protocol: HTTP
      TargetGroupAttributes:
        - Key: deregistration_delay.timeout_seconds
          Value: "60" 
      TargetType: !If
        - UseIPTargetType
        - ip
        - instance
      VpcId:
        Fn::ImportValue: !Join ["-", [!Ref VPCStackName, "VPCID"]]
  TargetGroup9:
    Type: AWS::ElasticLoadBalancingV2::TargetGroup
    Condition: TaskDefitionContainer9HasExposedPort
    Properties:
      HealthCheckIntervalSeconds: 60
      # will look for a 200 status code by default unless specified otherwise
      HealthCheckPath: !If
        - TaskDefitionContainer9HasHealthCheck
        - !Ref Container9HealthCheckPath
        - !Ref AWS::NoValue
      HealthCheckTimeoutSeconds: 10
      UnhealthyThresholdCount: 5
      HealthyThresholdCount: 2
      Matcher:
        HttpCode: !Ref MatcherHttpCode
      Name: !Join ["-", [!Ref Container9ContainerName, TG]]
      Port: !Ref Container9Port
      Protocol: HTTP
      TargetGroupAttributes:
        - Key: deregistration_delay.timeout_seconds
          Value: "60" 
      TargetType: !If
        - UseIPTargetType
        - ip
        - instance
      VpcId:
        Fn::ImportValue: !Join ["-", [!Ref VPCStackName, "VPCID"]]
  TargetGroup10:
    Type: AWS::ElasticLoadBalancingV2::TargetGroup
    Condition: TaskDefitionContainer10HasExposedPort
    Properties:
      HealthCheckIntervalSeconds: 60
      # will look for a 200 status code by default unless specified otherwise
      HealthCheckPath: !If
        - TaskDefitionContainer10HasHealthCheck
        - !Ref Container10HealthCheckPath
        - !Ref AWS::NoValue
      HealthCheckTimeoutSeconds: 10
      UnhealthyThresholdCount: 5
      HealthyThresholdCount: 2
      Matcher:
        HttpCode: !Ref MatcherHttpCode
      Name: !Join ["-", [!Ref Container10ContainerName, TG]]
      Port: !Ref Container10Port
      Protocol: HTTP
      TargetGroupAttributes:
        - Key: deregistration_delay.timeout_seconds
          Value: "60" 
      TargetType: !If
        - UseIPTargetType
        - ip
        - instance
      VpcId:
        Fn::ImportValue: !Join ["-", [!Ref VPCStackName, "VPCID"]]
  ListenerRule1:
    Type: AWS::ElasticLoadBalancingV2::ListenerRule
    Condition: TaskDefitionContainer1HasExposedPort
    Properties:
      Actions:
        - Type: forward
          TargetGroupArn: !Ref TargetGroup1
      Conditions:
        - Field: !Ref Container1ALBListenerRuleType
          HostHeaderConfig:
            Values:
              - !Sub "${Container1ALBListenerRuleValue}.*"
      ListenerArn: !If 
        - CreatePublicHTTPSListener
        - !Ref ListenerHTTPS
        - !Ref ListenerHTTP
      Priority: 1
  ListenerRule2:
    Type: AWS::ElasticLoadBalancingV2::ListenerRule
    Condition: TaskDefitionContainer2HasExposedPort
    Properties:
      Actions:
        - Type: forward
          TargetGroupArn: !Ref TargetGroup2
      Conditions:
        - Field: !Ref Container2ALBListenerRuleType
          HostHeaderConfig:
            Values:
              - !Sub "${Container2ALBListenerRuleValue}.*"
      ListenerArn: !If 
        - CreatePublicHTTPSListener
        - !Ref ListenerHTTPS
        - !Ref ListenerHTTP
      Priority: 2
  ListenerRule3:
    Type: AWS::ElasticLoadBalancingV2::ListenerRule
    Condition: TaskDefitionContainer3HasExposedPort
    Properties:
      Actions:
        - Type: forward
          TargetGroupArn: !Ref TargetGroup3
      Conditions:
        - Field: !Ref Container3ALBListenerRuleType
          HostHeaderConfig:
            Values:
              - !Sub "${Container3ALBListenerRuleValue}.*"
      ListenerArn: !If 
        - CreatePublicHTTPSListener
        - !Ref ListenerHTTPS
        - !Ref ListenerHTTP
      Priority: 3
  ListenerRule4:
    Type: AWS::ElasticLoadBalancingV2::ListenerRule
    Condition: TaskDefitionContainer4HasExposedPort
    Properties:
      Actions:
        - Type: forward
          TargetGroupArn: !Ref TargetGroup4
      Conditions:
        - Field: !Ref Container4ALBListenerRuleType
          HostHeaderConfig:
            Values:
              - !Sub "${Container4ALBListenerRuleValue}.*"
      ListenerArn: !If 
        - CreatePublicHTTPSListener
        - !Ref ListenerHTTPS
        - !Ref ListenerHTTP
      Priority: 4
  ListenerRule5:
    Type: AWS::ElasticLoadBalancingV2::ListenerRule
    Condition: TaskDefitionContainer5HasExposedPort
    Properties:
      Actions:
        - Type: forward
          TargetGroupArn: !Ref TargetGroup5
      Conditions:
        - Field: !Ref Container5ALBListenerRuleType
          HostHeaderConfig:
            Values:
              - !Sub "${Container5ALBListenerRuleValue}.*"
      ListenerArn: !If 
        - CreatePublicHTTPSListener
        - !Ref ListenerHTTPS
        - !Ref ListenerHTTP
      Priority: 5
  ListenerRule6:
    Type: AWS::ElasticLoadBalancingV2::ListenerRule
    Condition: TaskDefitionContainer6HasExposedPort
    Properties:
      Actions:
        - Type: forward
          TargetGroupArn: !Ref TargetGroup6
      Conditions:
        - Field: !Ref Container6ALBListenerRuleType
          HostHeaderConfig:
            Values:
              - !Sub "${Container6ALBListenerRuleValue}.*"
      ListenerArn: !If 
        - CreatePublicHTTPSListener
        - !Ref ListenerHTTPS
        - !Ref ListenerHTTP
      Priority: 6
  ListenerRule7:
    Type: AWS::ElasticLoadBalancingV2::ListenerRule
    Condition: TaskDefitionContainer7HasExposedPort
    Properties:
      Actions:
        - Type: forward
          TargetGroupArn: !Ref TargetGroup7
      Conditions:
        - Field: !Ref Container7ALBListenerRuleType
          HostHeaderConfig:
            Values:
              - !Sub "${Container7ALBListenerRuleValue}.*"
      ListenerArn: !If 
        - CreatePublicHTTPSListener
        - !Ref ListenerHTTPS
        - !Ref ListenerHTTP
      Priority: 7
  ListenerRule8:
    Type: AWS::ElasticLoadBalancingV2::ListenerRule
    Condition: TaskDefitionContainer8HasExposedPort
    Properties:
      Actions:
        - Type: forward
          TargetGroupArn: !Ref TargetGroup8
      Conditions:
        - Field: !Ref Container8ALBListenerRuleType
          HostHeaderConfig:
            Values:
              - !Sub "${Container8ALBListenerRuleValue}.*"
      ListenerArn: !If 
        - CreatePublicHTTPSListener
        - !Ref ListenerHTTPS
        - !Ref ListenerHTTP
      Priority: 8
  ListenerRule9:
    Type: AWS::ElasticLoadBalancingV2::ListenerRule
    Condition: TaskDefitionContainer9HasExposedPort
    Properties:
      Actions:
        - Type: forward
          TargetGroupArn: !Ref TargetGroup9
      Conditions:
        - Field: !Ref Container9ALBListenerRuleType
          HostHeaderConfig:
            Values:
              - !Sub "${Container9ALBListenerRuleValue}.*"
      ListenerArn: !If 
        - CreatePublicHTTPSListener
        - !Ref ListenerHTTPS
        - !Ref ListenerHTTP
      Priority: 9
  ListenerRule10:
    Type: AWS::ElasticLoadBalancingV2::ListenerRule
    Condition: TaskDefitionContainer10HasExposedPort
    Properties:
      Actions:
        - Type: forward
          TargetGroupArn: !Ref TargetGroup10
      Conditions:
        - Field: !Ref Container10ALBListenerRuleType
          HostHeaderConfig:
            Values:
              - !Sub "${Container10ALBListenerRuleValue}.*"
      ListenerArn: !If 
        - CreatePublicHTTPSListener
        - !Ref ListenerHTTPS
        - !Ref ListenerHTTP
      Priority: 10
  ListenerHTTP:
    Type: AWS::ElasticLoadBalancingV2::Listener
    Condition: CreatePublicHTTPListener
    DependsOn: LoadBalancer
    Properties:
      DefaultActions: !If 
        - CreatePublicHTTPSListener
        - - Type: "redirect"
            RedirectConfig:
              Protocol: "HTTPS"
              Port: 443
              Host: "#{host}"
              Path: "/#{path}"
              Query: "#{query}"
              StatusCode: "HTTP_301"
        - - TargetGroupArn: !Ref TargetGroup1
            Type: forward
      LoadBalancerArn: !Ref LoadBalancer
      Port: 80
      Protocol: HTTP
  ListenerHTTPS:
    Type: AWS::ElasticLoadBalancingV2::Listener
    Condition: CreatePublicHTTPSListener
    DependsOn: LoadBalancer
    Properties:
      DefaultActions: !If 
        - TaskDefitionContainer1HasExposedPort
        - - TargetGroupArn: !Ref TargetGroup1
            Type: forward
        - - FixedResponseConfig:
              ContentType: "text/plain"
              MessageBody: "No valid target defined"
              StatusCode: 200
      LoadBalancerArn: !Ref LoadBalancer
      Port: 443
      Protocol: HTTPS
      Certificates:
        - CertificateArn:
            Fn::ImportValue:
              !Join ["-", [!Ref CertificateStackName, "CertificateArn"]]
  LoadBalancer:
    Type: AWS::ElasticLoadBalancingV2::LoadBalancer
    Condition: CreateLoadBalancerCondition
    Properties:
      LoadBalancerAttributes:
        # this is the default, but is specified here in case it needs to be changed
        - Key: idle_timeout.timeout_seconds
          Value: "60"
        - Key: routing.http2.enabled
          Value: !Ref LoadBalancerEnableHttp2Routing
        - !If
          - LoadBalancerEnableLoggingCondition
          - Key: access_logs.s3.enabled
            Value: true
          - !Ref AWS::NoValue
        - !If
          - LoadBalancerEnableLoggingCondition
          - Key: access_logs.s3.bucket
            Value: !Ref S3LoadBalancerAccessLogsBucket
          - !Ref AWS::NoValue
        - !If
          - LoadBalancerEnableLoggingCondition
          - Key: access_logs.s3.prefix
            Value: Logs
          - !Ref AWS::NoValue
      Name: !Join ["", [!Ref ServiceName, LB]]
      # "internal" is also an option
      Scheme: internet-facing
      SecurityGroups:
        - !Ref LoadBalancerSecurityGroup
      Subnets:
        - Fn::ImportValue: !Join ["-", [!Ref VPCStackName, "PublicSubnet1ID"]]
        - Fn::ImportValue: !Join ["-", [!Ref VPCStackName, "PublicSubnet2ID"]]
  DNSRecord:
    Type: AWS::Route53::RecordSet
    Condition: CreateRoute53RecordSet
    DependsOn: LoadBalancer
    Properties:
      HostedZoneId: !Ref HostedZoneId
      Name: !Join ["", [!Ref Subdomain, ., !Ref HostedZoneName, .]]
      Type: A
      AliasTarget:
        DNSName: !GetAtt LoadBalancer.DNSName
        HostedZoneId: !GetAtt LoadBalancer.CanonicalHostedZoneID
  DNSRecord1:
    Type: AWS::Route53::RecordSet
    Condition: TaskDefitionContainer1HasExposedPort
    DependsOn: LoadBalancer
    Properties:
      HostedZoneId: !Ref HostedZoneId
      Name: !Sub "${Container1ALBListenerRuleValue}.${HostedZoneName}."
      Type: A
      AliasTarget:
        DNSName: !GetAtt LoadBalancer.DNSName
        HostedZoneId: !GetAtt LoadBalancer.CanonicalHostedZoneID
  DNSRecord2:
    Type: AWS::Route53::RecordSet
    Condition: TaskDefitionContainer2HasExposedPort
    DependsOn: LoadBalancer
    Properties:
      HostedZoneId: !Ref HostedZoneId
      Name: !Sub "${Container2ALBListenerRuleValue}.${HostedZoneName}."
      Type: A
      AliasTarget:
        DNSName: !GetAtt LoadBalancer.DNSName
        HostedZoneId: !GetAtt LoadBalancer.CanonicalHostedZoneID
  DNSRecord3:
    Type: AWS::Route53::RecordSet
    Condition: TaskDefitionContainer3HasExposedPort
    DependsOn: LoadBalancer
    Properties:
      HostedZoneId: !Ref HostedZoneId
      Name: !Sub "${Container3ALBListenerRuleValue}.${HostedZoneName}."
      Type: A
      AliasTarget:
        DNSName: !GetAtt LoadBalancer.DNSName
        HostedZoneId: !GetAtt LoadBalancer.CanonicalHostedZoneID
  DNSRecord4:
    Type: AWS::Route53::RecordSet
    Condition: TaskDefitionContainer4HasExposedPort
    DependsOn: LoadBalancer
    Properties:
      HostedZoneId: !Ref HostedZoneId
      Name: !Sub "${Container4ALBListenerRuleValue}.${HostedZoneName}."
      Type: A
      AliasTarget:
        DNSName: !GetAtt LoadBalancer.DNSName
        HostedZoneId: !GetAtt LoadBalancer.CanonicalHostedZoneID
  DNSRecord5:
    Type: AWS::Route53::RecordSet
    Condition: TaskDefitionContainer5HasExposedPort
    DependsOn: LoadBalancer
    Properties:
      HostedZoneId: !Ref HostedZoneId
      Name: !Sub "${Container5ALBListenerRuleValue}.${HostedZoneName}."
      Type: A
      AliasTarget:
        DNSName: !GetAtt LoadBalancer.DNSName
        HostedZoneId: !GetAtt LoadBalancer.CanonicalHostedZoneID
  DNSRecord6:
    Type: AWS::Route53::RecordSet
    Condition: TaskDefitionContainer6HasExposedPort
    DependsOn: LoadBalancer
    Properties:
      HostedZoneId: !Ref HostedZoneId
      Name: !Sub "${Container6ALBListenerRuleValue}.${HostedZoneName}."
      Type: A
      AliasTarget:
        DNSName: !GetAtt LoadBalancer.DNSName
        HostedZoneId: !GetAtt LoadBalancer.CanonicalHostedZoneID
  DNSRecord7:
    Type: AWS::Route53::RecordSet
    Condition: TaskDefitionContainer7HasExposedPort
    DependsOn: LoadBalancer
    Properties:
      HostedZoneId: !Ref HostedZoneId
      Name: !Sub "${Container7ALBListenerRuleValue}.${HostedZoneName}."
      Type: A
      AliasTarget:
        DNSName: !GetAtt LoadBalancer.DNSName
        HostedZoneId: !GetAtt LoadBalancer.CanonicalHostedZoneID
  DNSRecord8:
    Type: AWS::Route53::RecordSet
    Condition: TaskDefitionContainer8HasExposedPort
    DependsOn: LoadBalancer
    Properties:
      HostedZoneId: !Ref HostedZoneId
      Name: !Sub "${Container8ALBListenerRuleValue}.${HostedZoneName}."
      Type: A
      AliasTarget:
        DNSName: !GetAtt LoadBalancer.DNSName
        HostedZoneId: !GetAtt LoadBalancer.CanonicalHostedZoneID
  DNSRecord9:
    Type: AWS::Route53::RecordSet
    Condition: TaskDefitionContainer9HasExposedPort
    DependsOn: LoadBalancer
    Properties:
      HostedZoneId: !Ref HostedZoneId
      Name: !Sub "${Container9ALBListenerRuleValue}.${HostedZoneName}."
      Type: A
      AliasTarget:
        DNSName: !GetAtt LoadBalancer.DNSName
        HostedZoneId: !GetAtt LoadBalancer.CanonicalHostedZoneID
  DNSRecord10:
    Type: AWS::Route53::RecordSet
    Condition: TaskDefitionContainer10HasExposedPort
    DependsOn: LoadBalancer
    Properties:
      HostedZoneId: !Ref HostedZoneId
      Name: !Sub "${Container10ALBListenerRuleValue}.${HostedZoneName}."
      Type: A
      AliasTarget:
        DNSName: !GetAtt LoadBalancer.DNSName
        HostedZoneId: !GetAtt LoadBalancer.CanonicalHostedZoneID
  LogGroup:
    Type: AWS::Logs::LogGroup
    Properties:
      LogGroupName: !Join ["", [/ecs/, !Ref ServiceName, TaskDefinition]]
  FirelensLogGroup:
    Type: AWS::Logs::LogGroup
    Properties:
      LogGroupName:
        !Join ["", [/firelens-container/, !Ref ServiceName, TaskDefinition]]
  AutoScalingTarget:
    Type: AWS::ApplicationAutoScaling::ScalableTarget
    Properties:
      MinCapacity: !Ref MinTaskInstancesInService
      MaxCapacity: !Ref MaxTaskInstancesInService
      ResourceId: !Join
        - "/"
        - - "service"
          - Fn::ImportValue: !Join ["-", [!Ref ECSClusterStackName, "Cluster"]]
          - !GetAtt Service.Name
      ScalableDimension: ecs:service:DesiredCount
      ServiceNamespace: ecs
      # "The Amazon Resource Name (ARN) of an AWS Identity and Access Management (IAM) role that allows Application Auto Scaling to modify your scalable target."
      RoleARN: !GetAtt AutoScalingRole.Arn
  AutoScalingPolicy:
    Type: AWS::ApplicationAutoScaling::ScalingPolicy
    Properties:
      PolicyName: !Join ["", [!Ref ServiceName, AutoScalingPolicy]]
      PolicyType: TargetTrackingScaling
      ScalingTargetId: !Ref AutoScalingTarget
      TargetTrackingScalingPolicyConfiguration:
        PredefinedMetricSpecification:
          PredefinedMetricType: ECSServiceAverageCPUUtilization
        ScaleInCooldown: 10
        ScaleOutCooldown: 10
        # Keep things at or lower than 50% CPU utilization, for example
        TargetValue: !Ref AutoScalingTargetValue
  S3LoadBalancerAccessLogsBucket:
    Condition: LoadBalancerEnableLoggingCondition
    DeletionPolicy: Retain
    UpdateReplacePolicy: Retain
    Type: AWS::S3::Bucket
  S3LoadBalancerAccessLogsBucketPolicy:
    Condition: LoadBalancerEnableLoggingCondition
    Type: AWS::S3::BucketPolicy
    Properties:
      Bucket: !Ref S3LoadBalancerAccessLogsBucket
      PolicyDocument:
        Statement:
          - Sid: ELBAccessLogs20130930
            Effect: Allow
            Resource: !Join
              - ""
              - - !FindInMap
                  - RegionARNPrefix
                  - !Ref "AWS::Region"
                  - ARNPrefix
                - "s3:::"
                - !Ref "S3LoadBalancerAccessLogsBucket"
                - /
                - Logs
                - /AWSLogs/
                - !Ref "AWS::AccountId"
                - /*
            Principal:
              AWS:
                !FindInMap [RegionELBAccountId, !Ref "AWS::Region", AccountId]
            Action: ["s3:PutObject"]
Outputs:
  LoadBalancerDomainName:
    Condition: CreateLoadBalancerCondition
    Description: The DNS name for the load balancer.
    Value: !GetAtt LoadBalancer.DNSName
    Export:
      Name: !Sub "${AWS::StackName}-LoadBalancerDomainName"
